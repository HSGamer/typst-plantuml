[package]
name = "plantuml"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["HSGamer"]
license = "MIT"
description = "A Typst plugin to encode PlantUML diagrams for rendering via PlantUML server"
repository = "https://github.com/HSGamer/typst-plantuml"
categories = ["visualization"]
keywords = ["plantuml", "rust", "diagram", "visualization"]
